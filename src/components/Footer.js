import React from "react";

// Styles
import "./Footer.scss";

const Footer = () => {
  return (
    <footer>
      &copy; {new Date().getFullYear()} ELBEQQAL YOUSSEF
    </footer>
  );
};

export default Footer;
